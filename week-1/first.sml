(* This is a comment in ML  *)

val x = 34;

val y = 17;

val i = ~ 5;

val z = (x + y);

val s = "hello";

val t = if (y > (x + y)) then 0 else 1;

val t = 4; (* shadows previous t *)

val d = y;

val k = y + 1;